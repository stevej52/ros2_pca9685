# Snapshot of https://github.com/RobotX-Workshops/ros2-pca9685

- Upstream commit: `d4ede156f91bac38d782e41a15b64ec410619991` (2025-05-10)
- Copied: the whole repository
- Upstream license: Apache-2.0 (declared in README and package.xml, no LICENSE file upstream). The license of this snapshot is the upstream
  license, not the Apache-2.0 license of the ros2_pca9685 package.
- Why it is here: Python node with one raw-tick topic per channel on top of the legacy Adafruit_PCA9685 library.

This copy exists so the projects can be compared side by side; see
`comparison/README.md`. It is not built (`COLCON_IGNORE`) and not linted
(`AMENT_IGNORE`). Go upstream for the current version.
